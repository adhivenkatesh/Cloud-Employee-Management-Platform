apiVersion: v1
kind: Service

metadata:
  name: cemp-api-service

spec:

  selector:
    app: cemp-api

  type: NodePort

  ports:
  - protocol: TCP

    # Service Port
    port: 80

    # Container Port
    targetPort: 8080

    # Azure Browser Access Port
    nodePort: 30080
